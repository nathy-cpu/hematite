# Adapted from:
# https://github.com/duckdb/duckdb/blob/main/test/sql/order/negative_offset.test

statement ok
CREATE TABLE negative_limit_test (a INTEGER PRIMARY KEY);

statement ok
INSERT INTO negative_limit_test (a) VALUES (1), (2), (3);

statement error
SELECT a FROM negative_limit_test LIMIT -1;

statement error
SELECT a FROM negative_limit_test LIMIT 1 OFFSET -1;
