# Upstream reference:
# https://github.com/risinglightdb/sqllogictest-rs/blob/main/tests/slt/file_level_sort_mode.slt
#
# This file is intentionally skipped by the Hematite sqllogictest runner because
# it uses file-level control directives that we do not execute in the portable corpus.

control sortmode rowsort

query III
select * from example_sort
----
1 10 2333
10 100 2333
2 20 2333
